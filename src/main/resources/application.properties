spring.application.name=AssessmentApplication

# MySQL
# spring.datasource.url = jdbc:mysql://localhost:3306/userdb
# spring.datasource.username=root
# spring.datasource.password=V1gnesh@123
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

 # H2
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
# or for newer versions:
# logging.level.org.hibernate.orm.jdbc.bind=TRACE

#Observability
# 1. Expose all endpoints
management.endpoints.web.exposure.include=health,info,metrics,prometheus

# 2. Show full health details
management.endpoint.health.show-details=always

# 3. Enable Prometheus format
management.prometheus.metrics.export.enabled=true

# Logging to file
logging.file.name=logs/assessment-app.log
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

management.tracing.sampling.probability=1.0
management.otlp.tracing.endpoint=http://localhost:4318/v1/traces
logging.pattern.level=%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]

logging.level.com.example.assessmentapplication=DEBUG
